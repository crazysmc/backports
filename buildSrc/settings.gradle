dependencyResolutionManagement {
  versionCatalogs {
    libs {
      version 'loom', '1.5-SNAPSHOT'
      library 'loom', 'fabric-loom', 'fabric-loom.gradle.plugin' versionRef 'loom'
      library 'ploceus', 'ploceus', 'ploceus.gradle.plugin' versionRef 'loom'
    }
  }
}
